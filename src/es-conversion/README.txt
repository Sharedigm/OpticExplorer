These tools are used in the conversion between AMD modules and es modules.  The conversion tools don't convert the module paths correctly so we need to use these tools to fix the paths.

Step 1:
npm install

Step 2:
sh convert-amd-modules-to-es6.sh